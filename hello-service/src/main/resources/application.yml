spring:
  application:
    name: hello-service

server:
  port: 8000

grpc:
  port: 6000

eureka:
  instance:
    nonSecurePort: ${grpc.port}
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/